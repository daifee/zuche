import { StyleSheet } from 'react-native';
// import { FONT_SIZE } from '../../../styles';
import px from '../../../utils/px';

export const SideMenuContainer = StyleSheet.create({

  title: {
    fontSize: px(24)
  },
  desc: {
    fontSize: px(20),
    marginTop: px(6),
  }
});

