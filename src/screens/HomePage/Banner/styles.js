import { StyleSheet } from 'react-native';
import px from '../../../utils/px';

export const index = StyleSheet.create({
  container: {
    height: px(180),
  },

  image: {
    width: '100%',
    height: '100%',
  },
});

