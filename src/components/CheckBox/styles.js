import { StyleSheet } from 'react-native';

export const CheckBox = StyleSheet.create({
  container: {
    flex: 0,
    flexShrink: 0,
  },

  icon: {
    fontWeight: 'bold',
  },

  disable: {
    opacity: 0.6
  }
});

