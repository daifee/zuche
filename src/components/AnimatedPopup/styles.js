import { StyleSheet } from 'react-native';

export const AnimatedPopupBottom = StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    width: '100%',
    backgroundColor: '#fff'
  }
});


export const AnimatedPopupBottomContainer = StyleSheet.create({
  container: {
    //
  }
});
